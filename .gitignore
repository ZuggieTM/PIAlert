# Operating-system and editor files
.DS_Store
.idea/
.vscode/
.claude/
CLAUDE.md

# Libraries are supplied by the .pkgmeta externals at package time, so the
# working copies here are local-only.
Libs/

# Branding tooling: build-gallery.js requires sharp, which unpacks native
# binaries into node_modules/.
node_modules/
package-lock.json
